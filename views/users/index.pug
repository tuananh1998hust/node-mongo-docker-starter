h1  User Page
a(href="/users/create") Create New User

table
  tr
    th  Name
    th  Phone
    th  Email
  each user in users
    tr
      td  #{user.name}
      td  #{user.phone}
      td  #{user.email}